# codecademy Pokemon project

class Pokemon:
	#name=str, element=str, max_health=int, current_health=int, faint=bool
	__init__(self, name, element, max_health, current_health, faint):
	self.name=name
	self.element=element
	self.max_health=max_health
	self.current_health=current_health
	self.faint=faint